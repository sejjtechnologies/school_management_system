# Timetable System - Complete Workflow Diagram

## 1. APPLICATION STARTUP FLOW

```
┌─────────────────────────────────────────────┐
│   Application Starts (app.py)               │
└────────────────┬────────────────────────────┘
                 │
                 ▼
┌─────────────────────────────────────────────┐
│   Create Database Tables (if missing)       │
│   - TimeTableSlot                           │
│   - Class, Stream, Teacher, Subject         │
└────────────────┬────────────────────────────┘
                 │
                 ▼
┌─────────────────────────────────────────────┐
│   Check: Do timetables already exist?       │
│   Query: TimeTableSlot.query.first()        │
└────────────────┬────────────────────────────┘
                 │
        ┌────────┴────────┐
        │                 │
       YES               NO
        │                 │
        │                 ▼
        │    ┌────────────────────────────┐
        │    │ Auto-Generate Timetables   │
        │    │ For ALL Classes & Streams  │
        │    └────────────────┬───────────┘
        │                     │
        │                     ▼
        │    ┌────────────────────────────┐
        │    │ Generate Time Slots:       │
        │    │ - 8:00 AM to 5:00 PM      │
        │    │ - 40 min lessons           │
        │    │ - 20 min break @ 10 AM    │
        │    │ - 40 min lunch @ 1 PM     │
        │    │ - Mon to Sat               │
        │    └────────────────┬───────────┘
        │                     │
        │                     ▼
        │    ┌────────────────────────────┐
        │    │ Distribute Teachers:       │
        │    │ - Get all assigned         │
        │    │ - Include class teacher    │
        │    │ - Round-robin assign       │
        │    └────────────────┬───────────┘
        │                     │
        │                     ▼
        │    ┌────────────────────────────┐
        │    │ Save to Database           │
        │    │ TimeTableSlot records      │
        │    └────────────────┬───────────┘
        │                     │
        ▼                     ▼
┌─────────────────────────────────────────────┐
│   Application Ready                         │
│   Timetables loaded in database             │
└─────────────────────────────────────────────┘
```

---

## 2. USER VIEWING FLOW

```
┌─────────────────────────────────────────────┐
│   Admin Opens: /admin/timetable             │
└────────────────┬────────────────────────────┘
                 │
                 ▼
┌─────────────────────────────────────────────┐
│   Select Class Dropdown                     │
│   (e.g., "Primary 1 (P1)")                  │
└────────────────┬────────────────────────────┘
                 │
                 ▼
┌─────────────────────────────────────────────┐
│   Select Stream Dropdown                    │
│   (e.g., "Stream A", "Stream B", "Stream C")│
└────────────────┬────────────────────────────┘
                 │
                 ▼
┌─────────────────────────────────────────────┐
│   Click "Load Timetable" Button             │
└────────────────┬────────────────────────────┘
                 │
                 ▼
┌─────────────────────────────────────────────┐
│   Backend: GET /admin/timetable/get/<class>/<stream> │
└────────────────┬────────────────────────────┘
                 │
                 ▼
┌─────────────────────────────────────────────┐
│   Query Database:                           │
│   SELECT * FROM timetable_slots             │
│   WHERE class_id=X AND stream_id=Y          │
└────────────────┬────────────────────────────┘
                 │
                 ▼
┌─────────────────────────────────────────────┐
│   Return JSON with all slots                │
│   {                                         │
│     slots: [                                │
│       {id, teacher_name, subject_name,     │
│        day_of_week, start_time, end_time}  │
│     ]                                       │
│   }                                         │
└────────────────┬────────────────────────────┘
                 │
                 ▼
┌─────────────────────────────────────────────┐
│   Frontend: Render Timetable Grid           │
│   - 6 columns (Mon-Sat)                     │
│   - 15 rows (time slots)                    │
│   - Show teacher & subject per slot         │
│   - Display break & lunch with icons        │
└────────────────┬────────────────────────────┘
                 │
                 ▼
┌─────────────────────────────────────────────┐
│   Display Assigned Teachers Info            │
│   Show list of all teachers for this stream │
└─────────────────────────────────────────────┘
```

---

## 3. USER EDITING FLOW

```
┌─────────────────────────────────────────────┐
│   User Views Timetable                      │
│   (Already loaded from database)            │
└────────────────┬────────────────────────────┘
                 │
                 ▼
┌─────────────────────────────────────────────┐
│   Click "Edit" Button on Any Slot           │
└────────────────┬────────────────────────────┘
                 │
                 ▼
┌─────────────────────────────────────────────┐
│   Modal Opens: "Edit Timetable Slot"        │
│                                             │
│   ┌─────────────────────────────────────┐   │
│   │ Teacher:  [Dropdown - editable]     │   │
│   │ Subject:  [Dropdown - editable]     │   │
│   │                                     │   │
│   │ Day: Monday (DISPLAY ONLY)          │   │
│   │ Time: 8:00 AM (DISPLAY ONLY)        │   │
│   │                                     │   │
│   │ [Cancel] [Save Changes]             │   │
│   └─────────────────────────────────────┘   │
└────────────────┬────────────────────────────┘
                 │
                 ▼
┌─────────────────────────────────────────────┐
│   User Selects New Teacher and/or Subject   │
└────────────────┬────────────────────────────┘
                 │
                 ▼
┌─────────────────────────────────────────────┐
│   Click "Save Changes"                      │
└────────────────┬────────────────────────────┘
                 │
                 ▼
┌─────────────────────────────────────────────┐
│   Backend: PUT /admin/timetable/edit/<id>   │
│   Body: {                                   │
│     teacher_id: new_teacher_id,            │
│     subject_id: new_subject_id             │
│   }                                         │
└────────────────┬────────────────────────────┘
                 │
                 ▼
┌─────────────────────────────────────────────┐
│   Backend Validation:                       │
│   1. Check teacher not already assigned     │
│      to SAME STREAM at SAME TIME            │
│   2. Allow: teacher in different streams    │
└────────────────┬────────────────────────────┘
                 │
        ┌────────┴────────┐
        │                 │
     Valid            Invalid
        │                 │
        ▼                 ▼
    ┌───────────┐   ┌─────────────┐
    │ Update DB │   │ Return Error│
    │ Changes   │   │ Validation  │
    │ Saved ✓   │   │ Failed ✗    │
    └─────┬─────┘   └──────┬──────┘
          │                │
          ▼                ▼
    ┌────────────────────────────┐
    │ Return Success/Error JSON   │
    └────────┬───────────────────┘
             │
             ▼
    ┌────────────────────────────┐
    │ Frontend: Show Alert        │
    │ Success: "Slot updated ✓"   │
    │ Error: Show error message   │
    └────────┬───────────────────┘
             │
             ▼
    ┌────────────────────────────┐
    │ Reload Timetable from DB    │
    │ Grid updates automatically  │
    └────────────────────────────┘
```

---

## 4. ADMIN REGENERATION FLOW

```
┌──────────────────────────────────────────┐
│   Admin Runs CLI Command:                │
│   flask generate-timetables              │
└────────────────┬─────────────────────────┘
                 │
                 ▼
┌──────────────────────────────────────────┐
│   Backup Current Timetables (optional)   │
└────────────────┬─────────────────────────┘
                 │
                 ▼
┌──────────────────────────────────────────┐
│   DELETE ALL timetable_slots records      │
│   WHERE 1=1 (all records)                 │
└────────────────┬─────────────────────────┘
                 │
                 ▼
┌──────────────────────────────────────────┐
│   For Each Class:                         │
│     For Each Stream:                      │
│       Generate Time Slots                 │
│       Assign Teachers (round-robin)       │
│       Save to Database                    │
└────────────────┬─────────────────────────┘
                 │
                 ▼
┌──────────────────────────────────────────┐
│   Command Complete ✓                      │
│   "Timetables regenerated successfully!" │
└──────────────────────────────────────────┘
```

---

## 5. DATABASE OPERATIONS

### **Read (View Timetable)**
```sql
SELECT * FROM timetable_slots
WHERE class_id = ? AND stream_id = ?
ORDER BY day_of_week, start_time;
```

### **Create (Auto-generation)**
```sql
INSERT INTO timetable_slots 
(class_id, stream_id, teacher_id, subject_id, 
 day_of_week, start_time, end_time)
VALUES (?, ?, ?, ?, ?, ?, ?);
```

### **Update (Edit)**
```sql
UPDATE timetable_slots
SET teacher_id = ?, subject_id = ?
WHERE id = ?;
-- Note: day_of_week, start_time, end_time NEVER updated
```

### **Delete (Regeneration only)**
```sql
DELETE FROM timetable_slots
WHERE 1=1;  -- Clear all
```

---

## 6. CONSTRAINT VALIDATION

### **When Editing a Slot:**
```
NEW VALUE:
  teacher_id = 42
  subject_id = 15

VALIDATE:
  1. Is teacher 42 already assigned to 
     the SAME stream at SAME time?
     
  Query:
  SELECT * FROM timetable_slots
  WHERE teacher_id = 42
    AND stream_id = current_stream
    AND day_of_week = current_day
    AND start_time = current_time
    AND id != current_slot_id;
    
  If found: REJECT ✗ (return 409 Conflict)
  If not found: ACCEPT ✓ (save changes)
  
  NOTE: Teacher can teach DIFFERENT streams
        at the same time with different subjects
```

---

## 7. DATA FLOW DIAGRAM

```
DATABASE (PostgreSQL/Neon)
│
├── Users (Teachers)
├── Classes (P1, P2, ..., P7)
├── Streams (A, B, C per class)
├── Subjects (English, Math, etc.)
├── TeacherAssignments (Class → Teacher)
└── TimeTableSlots ⭐ (Auto-generated)
    ├── class_id (FK)
    ├── stream_id (FK)
    ├── teacher_id (FK)
    ├── subject_id (FK)
    ├── day_of_week
    ├── start_time
    ├── end_time
    └── Constraints:
        ├── unique(teacher, stream, day, time)
        └── unique(class, stream, day, time)

         │
         ▼
    BACKEND (Flask Routes)
    ├── GET  /admin/timetable/get/<class>/<stream>
    ├── GET  /admin/timetable/assigned-teachers/<class>/<stream>
    └── PUT  /admin/timetable/edit/<slot_id>

         │
         ▼
    FRONTEND (JavaScript)
    ├── loadTimetable()
    ├── renderTimetable()
    ├── openEditModal()
    └── saveSlot()

         │
         ▼
    USER (Admin/Teacher)
    └── Views & Edits Timetable
```

---

## 8. KEY CONSTRAINTS

| Constraint | Columns | Purpose |
|-----------|---------|---------|
| `unique_teacher_stream_slot` | teacher_id, stream_id, day_of_week, start_time | Prevent teacher double-booking on same stream |
| `unique_class_slot` | class_id, stream_id, day_of_week, start_time | Prevent class double-booking |

**Important:** Multi-stream teaching is ALLOWED
- Same teacher CAN teach different streams at same time
- Different subjects required per stream

---

## 9. TIME SCHEDULE BREAKDOWN

```
TIME           DURATION    TYPE                EDITABLE
─────────────────────────────────────────────────────────
8:00-8:40 AM   40 min     Lesson              ✓ Edit
8:40-9:20 AM   40 min     Lesson              ✓ Edit
9:20-10:00 AM  40 min     Lesson              ✓ Edit
10:00-10:20 AM 20 min     ☕ BREAK            ✗ NO EDIT
10:20-11:00 AM 40 min     Lesson              ✓ Edit
11:00-11:40 AM 40 min     Lesson              ✓ Edit
11:40-12:20 PM 40 min     Lesson              ✓ Edit
12:20-1:00 PM  40 min     Lesson              ✓ Edit
1:00-1:40 PM   40 min     🍽️ LUNCH            ✗ NO EDIT
1:40-2:20 PM   40 min     Lesson              ✓ Edit
2:20-3:00 PM   40 min     Lesson              ✓ Edit
3:00-3:40 PM   40 min     Lesson              ✓ Edit
3:40-4:20 PM   40 min     Lesson              ✓ Edit
4:20-5:00 PM   40 min     Lesson              ✓ Edit
─────────────────────────────────────────────────────────
TOTAL PER DAY:  9h         15 lessons + 2 breaks
TOTAL PER WEEK: 54h        90 lessons + 12 breaks
TOTAL PER TERM: 540h       900 lessons + 120 breaks
```

---

## 10. USER ROLE PERMISSIONS

### **Admin (Timetable Management)**
```
✅ View timetable
✅ Edit teacher on slot
✅ Edit subject on slot
✅ Refresh timetable
✅ Run: flask generate-timetables
❌ Add slot
❌ Delete slot
❌ Change day/time
```

### **Teacher (View Only)**
```
✅ View timetable (read-only)
✅ See their assigned slots
❌ Edit any slot
❌ Add slot
❌ Delete slot
```

### **Students/Parents**
```
❌ No access to admin timetable
(Can view via public schedule if implemented)
```

---

## 11. ERROR HANDLING

```
Error Scenario          HTTP Code   Message
──────────────────────────────────────────────────────
Teacher already assigned    409    "Teacher is already assigned 
to stream at this time             to this stream at [time] on 
                                   [day]"

Teacher not found          404    "Teacher not found"

Slot not found            404    "Slot not found"

Database error            500    "Internal server error"

Missing fields            400    "Missing required fields"

Invalid time format       400    "Invalid time format"
```

---

**System Architecture: Complete and Functional ✅**
